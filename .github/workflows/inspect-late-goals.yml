name: Inspect Late Goals

on:
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump per-match late-goal breakdown
        run: python scripts/inspect-late-goals.py
